function solution(arr) {
    return arr.reduce((acc, cur) => acc + cur) / arr.length;
    
//     let result = 0;
    
//     for(let v of arr) {
//         result += v;
//     }
    
//     return result / arr.length;
    
}